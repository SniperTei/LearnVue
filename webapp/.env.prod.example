# 生产环境配置示例
# 使用方法: cp .env.prod.example .env.prod
# 然后修改下面的配置项为实际值

NODE_ENV=production
VITE_APP_TITLE=Sniper Webapp

# API 配置
# ⚠️ 修改为你的实际后端 API 地址
# 示例：
# - 本地部署: VITE_APP_API_URL=https://your-domain.com
# - 使用子域名: VITE_APP_API_URL=https://api.your-domain.com
VITE_APP_API_URL=https://api-prod.sniper14.online

# 注意：
# 1. 生产环境建议使用 HTTPS
# 2. 如果前后端分离部署，填写后端的完整地址
# 3. 如果前后端同域，可以使用相对路径或同域地址
